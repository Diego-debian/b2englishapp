# Example environment variables for optional Cloudflare Tunnel. Copy this file to
# `.env` and replace with your own values if you plan to expose the services
# through Cloudflare. Without valid credentials the `cloudflared` service will
# simply do nothing.

# Cloudflare tunnel token provided by the Cloudflare dashboard
TUNNEL_TOKEN=