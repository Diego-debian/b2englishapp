# B2 English Verb Trainer — Frontend (Next.js)

## Requisitos
- Node.js 20+
- Backend corriendo (URL en `NEXT_PUBLIC_API_URL`)

## Setup (Windows)
1) Crea `.env.local` en la raíz:
```env
NEXT_PUBLIC_API_URL=http://localhost:8001
